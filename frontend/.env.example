# Copy this file to ".env" for local development.
# On Vercel, set this as an environment variable in the project dashboard
# instead (Settings -> Environment Variables), pointing at your deployed
# Render backend, e.g. https://reading-room-api.onrender.com

VITE_API_BASE_URL=http://localhost:8000
